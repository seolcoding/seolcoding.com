{
  "name": "seolcoding-portfolio",
  "version": "2.0.0",
  "description": "SeolCoding Portfolio Website - Hugo + Tailwind CSS",
  "private": true,
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify",
    "build:css": "echo \"CSS is built by Hugo Pipes during 'npm run dev' / 'npm run build'\"",
    "convert:pdf": "echo '📄 PDF files detected - no conversion needed for resume PDFs'"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.0"
  },
  "engines": {
    "node": ">=18"
  }
}
