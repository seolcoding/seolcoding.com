{
  "name": "seolcoding-portfolio",
  "version": "1.0.0",
  "description": "SeolCoding Portfolio with CareerCanvas theme",
  "scripts": {
    "dev": "hugo server -D",
    "build": "hugo --gc --minify",
    "build:css": "echo \"CSS is built by Hugo Pipes during 'npm run dev' / 'npm run build' (no theme writes).\"",
    "watch:css": "echo \"Run 'npm run dev' (Hugo) — CSS is rebuilt automatically by Hugo Pipes.\"",
    "convert:pdf": "node scripts/convert-pdfs.js"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "pdf-poppler": "^0.2.3",
    "postcss": "^8.4.32",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.0"
  }
}
