# Plan: 인터랙티브 튜토리얼 웹 기반 변환

## 프로젝트 개요

기존 Jupyter 노트북 기반 프롬프트 엔지니어링 튜토리얼을 웹 기반 인터랙티브 학습 플랫폼으로 변환

## 요구사항 정리

### P0 - 필수 (MVP)
1. **API 통합**: GPT-4.1 mini 기본, OpenRouter/오픈소스 모델 지원
2. **웹 기반 UI**: 인터랙티브한 웹 인터페이스, 모바일 반응형
3. **한국어 번역**: 전체 콘텐츠 한국어 지원
4. **프로필 시스템**: 로그인 없이 프로필 선택/생성
5. **진행상황 추적**: 개인별 학습 진행률 관리
6. **이어하기 기능**: 마지막 학습 지점에서 계속

### P1 - 추가 기능
7. **개인화 피드백**: 사용자 인터랙션 기반 LLM 피드백

---

## 현재 상태 분석

### 기존 콘텐츠
- **13개 Jupyter 노트북** (9개 메인 챕터 + 3개 부록 + How-To)
- **6,700줄+ 콘텐츠**
- **40+ 연습문제** + 정규식 기반 채점
- **힌트 시스템** (hints.py: 172줄)

### 챕터 구조
```
00. Tutorial How-To
01. Basic Prompt Structure
02. Being Clear and Direct
03. Assigning Roles (Role Prompting)
04. Separating Data and Instructions
05. Formatting Output and Speaking for Claude
06. Precognition (Thinking Step by Step)
07. Using Examples (Few-Shot Prompting)
08. Avoiding Hallucinations
09. Complex Prompts from Scratch
10.1 Appendix: Chaining Prompts
10.2 Appendix: Tool Use
10.3 Appendix: Search & Retrieval
```

---

## 기술 스택

### Frontend
| 기술 | 선택 이유 |
|------|-----------|
| Next.js 15 (App Router) | SSR/SSG, API Routes, 최신 React 기능 |
| Tailwind CSS | 빠른 반응형 디자인, 모바일 퍼스트 |
| shadcn/ui | 고품질 UI 컴포넌트, 커스터마이징 용이 |
| Zustand | 경량 상태관리, 간단한 API |
| next-intl | Next.js 최적화된 i18n |

### Backend/API
| 기술 | 선택 이유 |
|------|-----------|
| Next.js API Routes | 서버리스, 프론트엔드와 통합 |
| OpenAI SDK | GPT-4.1 mini 공식 SDK |
| OpenRouter 호환 | 다중 모델 지원 단일 인터페이스 |

### Storage
| 기술 | 선택 이유 |
|------|-----------|
| IndexedDB (Dexie.js) | 대용량 클라이언트 저장소 |
| localStorage | 간단한 설정 저장 |

### 코드 에디터
| 기술 | 선택 이유 |
|------|-----------|
| CodeMirror 6 | 경량, 모바일 친화적, 확장성 |

---

## 프로젝트 구조

```
prompt-eng-tutorial-web/
├── app/
│   ├── [locale]/                    # i18n 라우팅
│   │   ├── page.tsx                 # 홈/프로필 선택
│   │   ├── tutorial/
│   │   │   ├── page.tsx             # 튜토리얼 목차
│   │   │   └── [chapterId]/
│   │   │       └── page.tsx         # 챕터 상세
│   │   └── settings/
│   │       └── page.tsx             # API/언어 설정
│   ├── api/
│   │   ├── llm/
│   │   │   └── route.ts             # LLM API 프록시
│   │   └── feedback/
│   │       └── route.ts             # 개인화 피드백
│   └── layout.tsx
│
├── components/
│   ├── ui/                          # shadcn/ui
│   ├── layout/
│   │   ├── Header.tsx
│   │   ├── Sidebar.tsx
│   │   └── MobileNav.tsx
│   ├── profile/
│   │   ├── ProfileSelector.tsx
│   │   └── ProfileCard.tsx
│   ├── tutorial/
│   │   ├── LessonContent.tsx        # MDX 렌더링
│   │   ├── Exercise.tsx             # 연습문제
│   │   ├── CodePlayground.tsx       # 코드 실행
│   │   ├── PromptTester.tsx         # 프롬프트 테스트
│   │   ├── HintButton.tsx           # 힌트 표시
│   │   ├── GradingResult.tsx        # 채점 결과
│   │   └── ChapterProgress.tsx      # 챕터 진행률
│   └── feedback/
│       └── PersonalizedTips.tsx     # 개인화 피드백
│
├── lib/
│   ├── llm/
│   │   ├── index.ts                 # 통합 클라이언트
│   │   ├── openai.ts
│   │   ├── openrouter.ts
│   │   └── types.ts
│   ├── storage/
│   │   ├── db.ts                    # Dexie 설정
│   │   ├── profile.ts
│   │   ├── progress.ts
│   │   └── interactions.ts
│   ├── grading/
│   │   ├── index.ts                 # 채점 엔진
│   │   └── validators.ts            # 정규식 검증
│   └── utils/
│       └── encryption.ts            # API 키 암호화
│
├── content/
│   ├── chapters/
│   │   ├── en/
│   │   │   ├── 01-basic-prompt.mdx
│   │   │   └── ...
│   │   └── ko/
│   │       ├── 01-basic-prompt.mdx
│   │       └── ...
│   └── exercises/
│       ├── en/
│       │   └── exercises.json
│       └── ko/
│           └── exercises.json
│
├── i18n/
│   ├── config.ts
│   ├── en.json
│   └── ko.json
│
├── stores/
│   ├── profileStore.ts
│   ├── progressStore.ts
│   └── settingsStore.ts
│
├── types/
│   ├── profile.ts
│   ├── progress.ts
│   ├── content.ts
│   └── llm.ts
│
└── scripts/
    └── convert-notebooks.ts         # 노트북 변환 스크립트
```

---

## 데이터 스키마

### Profile
```typescript
interface Profile {
  id: string;                    // UUID
  name: string;                  // 표시 이름
  avatar?: string;               // 아바타 이모지/이미지
  createdAt: Date;
  lastActiveAt: Date;
  settings: ProfileSettings;
}

interface ProfileSettings {
  apiProvider: 'openai' | 'openrouter' | 'custom';
  apiKey?: string;               // 암호화 저장
  preferredModel: string;        // 기본: gpt-4.1-mini
  language: 'en' | 'ko';
  theme: 'light' | 'dark' | 'system';
}
```

### Progress
```typescript
interface ChapterProgress {
  profileId: string;
  chapterId: string;
  status: 'not_started' | 'in_progress' | 'completed';
  startedAt?: Date;
  completedAt?: Date;
  exercises: ExerciseProgress[];
}

interface ExerciseProgress {
  exerciseId: string;
  status: 'not_started' | 'attempted' | 'completed';
  attempts: number;
  bestScore: number;             // 0-100
  lastAttemptAt?: Date;
  userSolution?: string;         // 마지막 제출 답안
}
```

### Interaction (개인화 피드백용)
```typescript
interface Interaction {
  id: string;
  profileId: string;
  sessionId: string;
  timestamp: Date;
  type: 'prompt_test' | 'exercise_submit' | 'hint_view' | 'feedback_request';
  chapterId: string;
  exerciseId?: string;
  input: string;                 // 사용자 입력
  output?: string;               // LLM 응답
  score?: number;
  metadata?: Record<string, any>;
}
```

---

## 구현 단계

### Phase 1: 프로젝트 초기화 (1-2일)
- [ ] Next.js 15 프로젝트 생성
- [ ] Tailwind CSS + shadcn/ui 설정
- [ ] 기본 레이아웃 구조
- [ ] next-intl i18n 설정

### Phase 2: 프로필 시스템 (2-3일)
- [ ] IndexedDB 설정 (Dexie.js)
- [ ] 프로필 CRUD 로직
- [ ] 프로필 선택/생성 UI
- [ ] 설정 페이지 (API 키, 언어)

### Phase 3: 콘텐츠 변환 (3-4일)
- [ ] 노트북 → MDX 변환 스크립트
- [ ] 연습문제 JSON 추출
- [ ] 힌트 데이터 추출
- [ ] MDX 렌더링 컴포넌트

### Phase 4: 한국어 번역 (5-7일)
- [ ] UI 문자열 번역 (i18n JSON)
- [ ] 튜토리얼 콘텐츠 번역 (13개 챕터)
- [ ] 연습문제 번역
- [ ] 힌트 번역

### Phase 5: LLM API 통합 (2-3일)
- [ ] OpenAI 클라이언트 구현
- [ ] OpenRouter 클라이언트 구현
- [ ] API 프록시 라우트
- [ ] 에러 핸들링 및 로딩 상태

### Phase 6: 연습문제 시스템 (3-4일)
- [ ] 연습문제 컴포넌트
- [ ] 코드 에디터 (CodeMirror)
- [ ] 프롬프트 테스터
- [ ] 채점 로직 포팅
- [ ] 힌트 표시 UI

### Phase 7: 진행상황 추적 (2-3일)
- [ ] 진행상황 저장/로드
- [ ] 이어하기 기능
- [ ] 진행률 시각화
- [ ] 챕터별 완료 상태

### Phase 8: 개인화 피드백 (3-4일)
- [ ] 인터랙션 로깅
- [ ] 백그라운드 분석 로직
- [ ] 피드백 생성 프롬프트
- [ ] 피드백 UI 컴포넌트

### Phase 9: 모바일 최적화 (2-3일)
- [ ] 반응형 디자인 검증
- [ ] 터치 친화적 UI 조정
- [ ] 코드 에디터 모바일 최적화
- [ ] PWA manifest 설정

---

## 예상 리스크 및 대응

| 리스크 | 영향 | 대응 방안 |
|--------|------|-----------|
| API 키 보안 | 높음 | 암호화 저장 + 서버 프록시 |
| 번역 품질 | 중간 | LLM 초안 + 수동 검수 |
| 모바일 코드 에디터 | 중간 | CodeMirror 터치 최적화 |
| 기기간 동기화 | 낮음 | Phase 2로 Supabase 고려 |
| 대용량 인터랙션 로그 | 낮음 | 7일 이상 데이터 자동 정리 |

---

## 성공 지표 (Expected Outcomes)

| 지표 | 목표 |
|------|------|
| 전체 챕터 웹 변환 | 13/13 (100%) |
| 한국어 번역 완료 | 100% |
| 모바일 반응형 | 320px ~ 1920px |
| Lighthouse 성능 | > 90 |
| 연습문제 채점 정확도 | 기존과 동일 |
| 프로필 저장/로드 | 100% 신뢰성 |

---

---

## UI/UX 설계 원칙 (핵심 우선순위)

### 디자인 철학
> **"깔끔함 + 인터랙티브 = 학습 몰입"**

1. **미니멀리즘**: 불필요한 요소 제거, 콘텐츠에 집중
2. **인터랙티브 피드백**: 모든 액션에 즉각적인 시각적 반응
3. **점진적 공개**: 필요할 때 필요한 정보만 표시
4. **일관성**: 통일된 디자인 언어와 인터랙션 패턴

### 핵심 UI 컴포넌트 설계

#### 1. 프로필 선택 화면 (Landing)
```
┌─────────────────────────────────────────┐
│                                         │
│     🎓 프롬프트 엔지니어링 마스터       │
│                                         │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐ │
│  │  😊     │  │  🚀     │  │   +     │ │
│  │ User 1  │  │ User 2  │  │  새로   │ │
│  │ 45% 완료│  │ 12% 완료│  │  만들기 │ │
│  └─────────┘  └─────────┘  └─────────┘ │
│                                         │
│           [한국어 | English]            │
└─────────────────────────────────────────┘
```
- 프로필 카드 hover 시 부드러운 scale-up 애니메이션
- 진행률을 원형 프로그레스로 시각화
- 클릭 시 ripple 효과

#### 2. 튜토리얼 목차 (Chapter List)
```
┌─────────────────────────────────────────┐
│ ← 뒤로    📚 챕터 목록         ⚙️ 설정 │
├─────────────────────────────────────────┤
│                                         │
│  전체 진행률 ████████░░ 68%             │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ ✅ 01. 기본 프롬프트 구조          │  │
│  │    "프롬프트의 기본 구조를 배웁니다" │  │
│  │    ████████████ 완료              │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 🔄 02. 명확하고 직접적으로         │  │
│  │    "명확한 지시사항 작성법"        │  │
│  │    ████████░░░░ 67%               │  │
│  │    [이어하기 →]                    │  │
│  └───────────────────────────────────┘  │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │ 🔒 03. 역할 부여하기               │  │
│  │    완료 후 잠금 해제               │  │
│  └───────────────────────────────────┘  │
│                                         │
└─────────────────────────────────────────┘
```
- 스크롤 시 부드러운 스티키 헤더
- 챕터 카드 입장 시 staggered 애니메이션
- 잠긴 챕터는 blur + overlay 처리

#### 3. 챕터 학습 화면 (Main Learning View)
```
┌─────────────────────────────────────────────────────────┐
│ ← 목차    02. 명확하고 직접적으로    진행률 3/5    ⚙️   │
├─────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────┐ │
│ │                                                     │ │
│ │  ## 레슨: 명확한 지시사항의 중요성                  │ │
│ │                                                     │ │
│ │  Claude에게 원하는 결과를 얻으려면 명확하게        │ │
│ │  지시해야 합니다. 마치 새로운 직원에게...          │ │
│ │                                                     │ │
│ │  💡 핵심 포인트                                    │ │
│ │  ┌─────────────────────────────────────┐           │ │
│ │  │ 1. 구체적인 단어 사용                │           │ │
│ │  │ 2. 예상 출력 형식 명시               │           │ │
│ │  │ 3. 모호한 표현 피하기                │           │ │
│ │  └─────────────────────────────────────┘           │ │
│ │                                                     │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│ ┌─────────────────────────────────────────────────────┐ │
│ │  🧪 예제 플레이그라운드                             │ │
│ │  ┌───────────────────────────────────────────────┐ │ │
│ │  │ System: You are a helpful assistant.          │ │ │
│ │  │                                               │ │ │
│ │  │ User: 날씨에 대해 알려줘                      │ │ │
│ │  │                                         [▶️]  │ │ │
│ │  └───────────────────────────────────────────────┘ │ │
│ │                                                     │ │
│ │  ┌───────────────────────────────────────────────┐ │ │
│ │  │ Claude 응답:                                  │ │ │
│ │  │ ░░░░░░░░░░░░ (실행을 눌러주세요)              │ │ │
│ │  └───────────────────────────────────────────────┘ │ │
│ └─────────────────────────────────────────────────────┘ │
│                                                         │
│  ┌─────────────┐                    ┌───────────────┐   │
│  │  ← 이전     │                    │   다음 →      │   │
│  └─────────────┘                    └───────────────┘   │
└─────────────────────────────────────────────────────────┘
```

#### 4. 연습문제 UI
```
┌─────────────────────────────────────────────────────────┐
│  📝 연습문제 2/3                                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  문제: 다음 프롬프트를 개선하여 더 구체적인            │
│  응답을 받을 수 있도록 수정하세요.                     │
│                                                         │
│  원본 프롬프트:                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │ "글 써줘"                                         │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  개선된 프롬프트:                                       │
│  ┌───────────────────────────────────────────────────┐ │
│  │                                                   │ │
│  │ _                                                 │ │
│  │                                                   │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐  │
│  │  💡 힌트     │  │  ▶️ 테스트   │  │  ✅ 제출     │  │
│  └──────────────┘  └──────────────┘  └──────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐ │
│  │  채점 결과:                                       │ │
│  │  ✅ 구체적인 주제 포함                            │ │
│  │  ✅ 글의 형식 명시                                │ │
│  │  ⚠️ 길이 지정 권장                               │ │
│  │                                                   │ │
│  │  점수: 85/100                                     │ │
│  └───────────────────────────────────────────────────┘ │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### 인터랙션 & 애니메이션 명세

#### 마이크로 인터랙션
| 요소 | 인터랙션 | 애니메이션 |
|------|----------|------------|
| 버튼 | hover/click | scale(1.02) + shadow 증가, ripple 효과 |
| 카드 | hover | translateY(-4px) + shadow 강화 |
| 프로그레스 바 | 로딩 | 좌→우 부드러운 채움 (ease-out) |
| 체크마크 | 완료 시 | 원형 → 체크 SVG path 애니메이션 |
| 토스트 | 표시/숨김 | slide-up + fade (spring physics) |
| 모달 | 열기/닫기 | scale(0.95→1) + backdrop blur |
| 코드 실행 | 로딩 | 점 3개 typing 애니메이션 |

#### 페이지 전환
- Next.js App Router의 `loading.tsx` 활용
- 스켈레톤 UI로 콘텐츠 로딩 표시
- 부드러운 fade-in 전환 (150ms)

#### 피드백 시스템
| 상태 | 시각적 피드백 | 설명 |
|------|--------------|------|
| 성공 | 녹색 체크 + confetti | 연습문제 통과 시 |
| 부분 성공 | 노란 경고 + 개선점 | 점수 70-89% |
| 실패 | 빨간 X + 힌트 제안 | 점수 < 70% |
| 로딩 | Spinner + skeleton | API 호출 중 |

### 반응형 브레이크포인트

```css
/* Mobile First */
/* sm: 640px  - 스마트폰 가로 */
/* md: 768px  - 태블릿 세로 */
/* lg: 1024px - 태블릿 가로 / 노트북 */
/* xl: 1280px - 데스크탑 */
```

| 브레이크포인트 | 레이아웃 변화 |
|---------------|--------------|
| < 640px | 단일 컬럼, 하단 네비게이션, 전체화면 코드 에디터 |
| 640-1024px | 사이드바 토글, 2컬럼 레이아웃 |
| > 1024px | 고정 사이드바, 넓은 코드 에디터 |

### 컬러 시스템

```typescript
const colors = {
  // Primary - 학습/진행
  primary: {
    50: '#eff6ff',
    500: '#3b82f6',  // 메인 액션
    600: '#2563eb',  // hover
  },
  // Success - 완료/정답
  success: {
    50: '#f0fdf4',
    500: '#22c55e',
  },
  // Warning - 부분/힌트
  warning: {
    50: '#fefce8',
    500: '#eab308',
  },
  // Error - 오류/재시도
  error: {
    50: '#fef2f2',
    500: '#ef4444',
  },
  // Neutral - 배경/텍스트
  neutral: {
    50: '#fafafa',
    100: '#f5f5f5',
    800: '#262626',
    900: '#171717',
  }
}
```

### 타이포그래피

```typescript
const typography = {
  // 한국어 최적화 폰트
  fontFamily: {
    sans: ['Pretendard', 'sans-serif'],
    mono: ['JetBrains Mono', 'monospace'],
  },
  fontSize: {
    xs: '0.75rem',   // 12px - 보조 텍스트
    sm: '0.875rem',  // 14px - 본문 작은
    base: '1rem',    // 16px - 본문
    lg: '1.125rem',  // 18px - 소제목
    xl: '1.25rem',   // 20px - 제목
    '2xl': '1.5rem', // 24px - 챕터 제목
  }
}
```

### 접근성 (a11y)

- WCAG 2.1 AA 준수
- 키보드 네비게이션 완벽 지원
- 스크린 리더 호환 (aria-labels)
- 색맹 대응 (아이콘 + 색상 조합)
- 최소 터치 타겟 44x44px

---

## 다음 단계

1. 사용자 승인 후 Phase 1 시작
2. Next.js 프로젝트 초기화
3. 기본 레이아웃 구현
